import { PlatformsArrayPipe } from './platforms-array.pipe';

describe('PlatformsArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new PlatformsArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
